using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UiSettingManager : MonoBehaviour
{
    [Header("控制的mover")]
    [SerializeField] private UiMover volumeMover;
    [SerializeField] private UiMover infoMover;
    [SerializeField] private UiMover collectMover;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
